# coding: utf-8

"""
    Cohesity REST API

    Cohesity API provides a RESTful interface to access the various data management operations on Cohesity cluster and Helios.

    The version of the OpenAPI document: 2.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Set
from typing_extensions import Self

class IbmTenantMeteringConfig(BaseModel):
    """
    Specifies the metering configuration that will be used for cohesity cluster to send the billing details to IBM billing service.
    """ # noqa: E501
    part_ids: Optional[Annotated[List[StrictStr], Field(min_length=1)]] = Field(default=None, description="Specifies the list of part identifiers used for metrics identification.", alias="partIds")
    submission_interval_in_secs: Optional[StrictInt] = Field(default=None, description="Specifies the frequency in seconds at which the metrics will be pushed to IBM billing service from cluster.", alias="submissionIntervalInSecs")
    url: Optional[StrictStr] = Field(default=None, description="Specifies the base metering URL that will be used by cluster to send the billing information.")
    __properties: ClassVar[List[str]] = ["partIds", "submissionIntervalInSecs", "url"]

    @field_validator('part_ids')
    def part_ids_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in set(['USAGETERABYTE']):
                raise ValueError("each list item must be one of ('USAGETERABYTE')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of IbmTenantMeteringConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if submission_interval_in_secs (nullable) is None
        # and model_fields_set contains the field
        if self.submission_interval_in_secs is None and "submission_interval_in_secs" in self.model_fields_set:
            _dict['submissionIntervalInSecs'] = None

        # set to None if url (nullable) is None
        # and model_fields_set contains the field
        if self.url is None and "url" in self.model_fields_set:
            _dict['url'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of IbmTenantMeteringConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "partIds": obj.get("partIds"),
            "submissionIntervalInSecs": obj.get("submissionIntervalInSecs"),
            "url": obj.get("url")
        })
        return _obj


