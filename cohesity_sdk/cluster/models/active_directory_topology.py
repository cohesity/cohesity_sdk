# coding: utf-8

"""
    Cohesity REST API

    Cohesity API provides a RESTful interface to access the various data management operations on Cohesity cluster and Helios.

    The version of the OpenAPI document: 2.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from cohesity_sdk.cluster.models.member_domain import MemberDomain
from cohesity_sdk.cluster.models.primary_domain import PrimaryDomain
from typing import Set
from typing_extensions import Self

class ActiveDirectoryTopology(BaseModel):
    """
    Response of Active directory topology.
    """ # noqa: E501
    member_domains: Optional[Dict[str, MemberDomain]] = Field(default=None, description="Specifies map of domains where the key represents FQDN (fully qualified domain name) of each domain. The map contains both primary and trusted domains.", alias="memberDomains")
    primary_domains: Optional[Dict[str, PrimaryDomain]] = Field(default=None, description="Specifies map of primary domains where the key represents FQDN (fully qualified domain name) of each primary domain.", alias="primaryDomains")
    __properties: ClassVar[List[str]] = ["memberDomains", "primaryDomains"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ActiveDirectoryTopology from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each value in member_domains (dict)
        _field_dict = {}
        if self.member_domains:
            for _key_member_domains in self.member_domains:
                if self.member_domains[_key_member_domains]:
                    _field_dict[_key_member_domains] = self.member_domains[_key_member_domains].to_dict()
            _dict['memberDomains'] = _field_dict
        # override the default output from pydantic by calling `to_dict()` of each value in primary_domains (dict)
        _field_dict = {}
        if self.primary_domains:
            for _key_primary_domains in self.primary_domains:
                if self.primary_domains[_key_primary_domains]:
                    _field_dict[_key_primary_domains] = self.primary_domains[_key_primary_domains].to_dict()
            _dict['primaryDomains'] = _field_dict
        # set to None if member_domains (nullable) is None
        # and model_fields_set contains the field
        if self.member_domains is None and "member_domains" in self.model_fields_set:
            _dict['memberDomains'] = None

        # set to None if primary_domains (nullable) is None
        # and model_fields_set contains the field
        if self.primary_domains is None and "primary_domains" in self.model_fields_set:
            _dict['primaryDomains'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ActiveDirectoryTopology from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "memberDomains": dict(
                (_k, MemberDomain.from_dict(_v))
                for _k, _v in obj["memberDomains"].items()
            )
            if obj.get("memberDomains") is not None
            else None,
            "primaryDomains": dict(
                (_k, PrimaryDomain.from_dict(_v))
                for _k, _v in obj["primaryDomains"].items()
            )
            if obj.get("primaryDomains") is not None
            else None
        })
        return _obj


